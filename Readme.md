index.html is the main page so run it on that page in the browser.
use live server extentsion to easily run it on browser careful,
because you need to start the live server in the index.html page so the page could see the changes you made ...!
